[project]
name = "weather-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vasilisa", email = "277248801+Vasilisa-6-6@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pytelegrambotapi>=4.36.1",
    "pyttsx3>=2.99",
]

[project.scripts]
weather-bot = "weather_bot:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
